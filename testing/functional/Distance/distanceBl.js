module.exports = {
    //Find And Print distance between point ti origin

    distance: (x2, y2) => {
        var x2=2;
        var y2=1;
        return Math.sqrt(Math.pow(x2, 2) + Math.pow(y2, 2));
    }
}
